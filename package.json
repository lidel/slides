{
  "name": "ipfs-in-web-browsers-berlin-2018",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html --public-url ./",
    "test": "standard",
    "deploy": "npm run build; npm run ipfs",
    "ipfs": "ipfs add --cid-version 1 -r -Q dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^1.9.3",
    "standard": "^11.0.1"
  },
  "dependencies": {
    "ipfs-css": "^0.5.2",
    "tachyons": "^4.10.0"
  }
}
